# web — unified web client

The single web app that replaces Wolf's three fragmented surfaces (WolfUI launcher + WolfManager + WolfDen): library, lobbies, account/settings, and the live stream view as routes in one codebase. Talks to the control plane and connects to sessions over WebRTC.

Built in Phase 1, growing out of `../spike/client`.
