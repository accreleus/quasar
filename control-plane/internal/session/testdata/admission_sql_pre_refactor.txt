candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND NOT EXISTS ( SELECT 1 FROM installed_images ii WHERE (ii.registry_ref = $3 OR ii.local_tag = $3) AND NOT EXISTS ( SELECT 1 FROM host_images hi WHERE hi.host_id = g.host_id AND hi.image_id = ii.image_id AND hi.state = 'ready' AND (hi.version = '' OR hi.version = ii.version) ) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND h.id = $3::uuid AND NOT EXISTS ( SELECT 1 FROM installed_images ii WHERE (ii.registry_ref = $4 OR ii.local_tag = $4) AND NOT EXISTS ( SELECT 1 FROM host_images hi WHERE hi.host_id = g.host_id AND hi.image_id = ii.image_id AND hi.state = 'ready' AND (hi.version = '' OR hi.version = ii.version) ) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND h.id = $3::uuid GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND h.id = $5::uuid GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 AND ( g.vram_mb_free IS NULL OR g.vram_sampled_at IS NULL OR g.vram_sampled_at < now() - make_interval(secs => $2::int) OR g.vram_mb_total <= $3 OR g.vram_mb_free - ( SELECT COUNT(*) * $4 FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $2::int)) ) >= $3 ) ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 AND ( g.vram_mb_free IS NULL OR g.vram_sampled_at IS NULL OR g.vram_sampled_at < now() - make_interval(secs => $2::int) OR g.vram_mb_total <= $3 OR g.vram_mb_free - ( SELECT COUNT(*) * $4 FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $2::int)) ) >= $3 ) ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 AND ( g.vram_mb_free IS NULL OR g.vram_sampled_at IS NULL OR g.vram_sampled_at < now() - make_interval(secs => $2::int) OR g.vram_mb_total <= $3 OR g.vram_mb_free - ( SELECT COUNT(*) * $4 FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $2::int)) ) >= $3 ) ORDER BY CASE WHEN g.host_id = ( SELECT host_id FROM user_homes WHERE user_id = $5::uuid AND app_id = $6::uuid AND gc_after IS NULL ORDER BY last_used_at DESC LIMIT 1 ) THEN 0 ELSE 1 END ASC, (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
candidate	SELECT g.id::text, g.host_id::text, g.index FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY CASE WHEN g.host_id = ( SELECT host_id FROM user_homes WHERE user_id = $3::uuid AND app_id = $4::uuid AND gc_after IS NULL ORDER BY last_used_at DESC LIMIT 1 ) THEN 0 ELSE 1 END ASC, (g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0)) DESC, CASE WHEN g.vram_sampled_at > now() - make_interval(secs => $2::int) THEN g.vram_mb_free END DESC NULLS LAST, COUNT(s.id) ASC, g.id ASC LIMIT 1
recheck	SELECT h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND g.encode_slots_total - COALESCE((SELECT SUM(x.reserved_encode_slots) FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping')), 0) >= $2 AND ( g.vram_mb_free IS NULL OR g.vram_sampled_at IS NULL OR g.vram_sampled_at < now() - make_interval(secs => $3::int) OR g.vram_mb_total <= $4 OR g.vram_mb_free - ( SELECT COUNT(*) * $5 FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $3::int)) ) >= $4 ) FROM gpus g JOIN hosts h ON h.id = g.host_id WHERE g.id = $1::uuid AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) FOR UPDATE OF h, g
recheck	SELECT h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND g.encode_slots_total - COALESCE((SELECT SUM(x.reserved_encode_slots) FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping')), 0) >= $2 AND NOT EXISTS ( SELECT 1 FROM installed_images ii WHERE (ii.registry_ref = $3 OR ii.local_tag = $3) AND NOT EXISTS ( SELECT 1 FROM host_images hi WHERE hi.host_id = g.host_id AND hi.image_id = ii.image_id AND hi.state = 'ready' AND (hi.version = '' OR hi.version = ii.version) ) ) FROM gpus g JOIN hosts h ON h.id = g.host_id WHERE g.id = $1::uuid AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) FOR UPDATE OF h, g
recheck	SELECT h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND g.encode_slots_total - COALESCE((SELECT SUM(x.reserved_encode_slots) FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping')), 0) >= $2 FROM gpus g JOIN hosts h ON h.id = g.host_id WHERE g.id = $1::uuid AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) FOR UPDATE OF h, g
totals	SELECT EXISTS ( SELECT 1 FROM gpus g JOIN hosts h ON h.id = g.host_id WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND NOT EXISTS ( SELECT 1 FROM installed_images ii WHERE (ii.registry_ref = $2 OR ii.local_tag = $2) AND NOT EXISTS ( SELECT 1 FROM host_images hi WHERE hi.host_id = g.host_id AND hi.image_id = ii.image_id AND hi.state = 'ready' AND (hi.version = '' OR hi.version = ii.version) ) ) AND g.encode_slots_total >= $1 )
totals	SELECT EXISTS ( SELECT 1 FROM gpus g JOIN hosts h ON h.id = g.host_id WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND g.encode_slots_total >= $1 )
vetodiag	SELECT g.id::text, g.host_id::text, g.index, g.vram_mb_total, g.vram_mb_free, g.vram_sampled_at, (EXTRACT(EPOCH FROM (now() - g.vram_sampled_at)) * 1000)::bigint, (SELECT COUNT(*) FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $2::int)))::int FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) AND h.id = $3::uuid GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY g.id LIMIT 16
vetodiag	SELECT g.id::text, g.host_id::text, g.index, g.vram_mb_total, g.vram_mb_free, g.vram_sampled_at, (EXTRACT(EPOCH FROM (now() - g.vram_sampled_at)) * 1000)::bigint, (SELECT COUNT(*) FROM sessions x WHERE x.gpu_id = g.id AND x.state IN ('assigned','starting','running','stopping') AND (x.started_at IS NULL OR x.started_at > g.vram_sampled_at - make_interval(secs => $2::int)))::int FROM gpus g JOIN hosts h ON h.id = g.host_id LEFT JOIN sessions s ON s.gpu_id = g.id AND s.state IN ('assigned','starting','running','stopping') WHERE h.status = 'online' AND h.capacity_detection = 'ok' AND g.reported AND ( COALESCE(h.effective_settings->>'encoder', '') = '' OR h.effective_settings->>'encoder' = 'openh264' OR (h.effective_settings->>'encoder' = 'va' AND lower(g.vendor) IN ('amd','intel') AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'nvenc' AND lower(g.vendor) = 'nvidia' AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) OR (h.effective_settings->>'encoder' = 'vulkan' AND g.index = 0 AND (COALESCE(h.effective_settings->>'render_node', '') = '' OR g.render_node = h.effective_settings->>'render_node' OR g.device_path = h.effective_settings->>'render_node')) ) GROUP BY g.id HAVING g.encode_slots_total - COALESCE(SUM(s.reserved_encode_slots), 0) >= $1 ORDER BY g.id LIMIT 16
