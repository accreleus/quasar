# Next prompt: #240 integrated acceptance

Use this after #251 acceptance is complete. Do not start #240 as part of #251.

```text
$implement #240

Build on completed #230–#239 and #251. Read the #239 CLI-removal/recovery
acceptance report and docs/reports/2026-09-17-rh01-251-reconnect-stop-acceptance.md.
Follow #240 and the approved #228/#229 contracts and research indexed by #224.
Do not reopen settled runtime architecture decisions.

Work on feature/rh-01-runtime-api; integration only into
initiative/resilient-host-architecture. Preserve existing work and deployed
stacks. Do not merge to develop/main, create release tags, or trigger Actions
image builds. Promotion requires explicit owner approval after the evidence
is ready for review.

First follow AGENTS.md “Shared-host version preflight” on both targets. Record
the observed baseline and resolve component/schema mismatches before testing.

Validate the integrated candidate, including fresh deployment, real Docker
lifecycle/image/ownership/capability/failure behavior, concurrent GPU sessions,
switching, image operations and bounded teardown. Exercise daemon outages,
interrupted mutations, observation cancellation, uncertain outcomes and durable
retryable cleanup. Never retry an uncertain mutation under a new operation
identity or mix CLI/API ownership. Do not add automatic CLI fallback.

Retain the #251 regression: running session → agent reconnect → user stop before
first heartbeat → explicit heartbeat omission. Show stopped/host_lost without
another reconnect, no application-failure report, independently completed
cleanup, and safe reuse of the preserved managed home. Check slow teardown,
missing versus empty heartbeat lists, stale connection fencing and shared-home
exclusion through stopping. Heartbeat absence does not prove container deletion.
Preserve the agent's pending-operation and startup-retirement gates.

Delegation is authorized: at most one gpt-5.6-terra implementation worker and
gpt-5.6-sol independent specification reviewer. Primary owns coordination,
standards/correctness review, fixed-diff review, integration, validation and
publication. Workers must not commit, push, merge or update tickets. Report
unavailable requested models rather than silently substituting.

Build required images locally through deploy/build-images.sh and run the
unchanged image contract. Record exact reviewed source, image IDs, engine/API/
client versions and relevant settings before deployment. For Go-only corrections,
use the canonical control-plane deployment path without recreating agents.

Bounded validation is authorized on the isolated local AMD stack and the private
gpu-test NVIDIA role resolved through existing tooling. Verify the target is idle
before recreating its agent. Preserve identity, user data, volumes and unrelated
workloads. The dedicated NVIDIA test deployment may be cleared if needed;
that permission does not apply to the local AMD host. Never broadly prune the local AMD host, restart
its Docker daemon, change drivers or inject host-wide faults. Exercise outage
semantics through a narrowly owned engine endpoint/proxy where appropriate.

Demonstrate actual AMD VA/Vulkan encoding and NVIDIA rendering/encoding with
the existing driver volume. On both, validate rendered application content,
browser audio and application-visible keyboard/mouse effects. Device listings,
registered elements, an open input channel and decoded black frames are not
successful session validation. Record the actual encoder. Use disposable owned
assets for failed launch, isolation and managed-home recovery checks.

Demonstrate return to a recorded known-good image with identity and user data
preserved; draining sessions and redeployment are permitted. Account for the
#251 control-plane schema-84 baseline: do not use the former schema-80
control-plane image against the upgraded database. Restore temporary
settings and remove owned test assets. Keep the accepted Intel paths and
external test procedure; unavailable Intel hardware is not AMD/NVIDIA failure
and must not be presented as certification. Full Podman/rootless certification,
updater redesign, running-session adoption, TURN and roaming remain out of scope.

Run make verify, make test-rust, affected component checks, and make preflight
for the exact promotion candidate. Go/database changes also require make test-go
and make test-db. Resolve review defects and repeat affected acceptance after
corrections. Do not defer #240 acceptance or impose unattended time/usage budgets.

Commit and push validated changes to the authorized branches. Attach sanitized
evidence and close #240 only when its acceptance criteria pass. Finish with
changes/commits, exact deployed candidate identities, test and AMD/NVIDIA results,
recovery evidence, review findings, limitations and a concrete promotion decision
for owner approval. Do not promote or begin another issue automatically.
```
