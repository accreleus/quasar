# scripts/dev — contributor tooling

The dev-container wrapper (`dev.sh`), the compose-overlay test, the volume migrator, the local-audio validator, the development catalog seeders, and the leak-scan push guard. Catalogue: `docs/developer-tooling.md`.
