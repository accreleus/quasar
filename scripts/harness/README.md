# scripts/harness — acceptance and diagnostic harnesses

The `run-*.sh` harnesses, their shared PASS/FAIL/SKIP core (`lib/harness.sh`), read-only `checks/`, the `apitest` OpenAPI-conformance module, and `peer-driver.mjs` (the headless WebRTC peer they drive). Catalogue: `docs/developer-tooling.md`.
